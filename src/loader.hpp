#include "common.hpp"

namespace fileManagement{
    //load Dialogue, Save
    void loadAllFile(const char* dialogue, const char* save, common::Game &game);
    //unload all file
    void unloadAllFile();

}